package partials

import "github.com/indaco/goaster/examples/styles"

templ Head(title string) {
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>{ title }</title>
		<!-- styles -->
		@styles.Base()
		{ children... }
	</head>
}
